"""
routing.py — road-routed distance via OpenRouteService, with a haversine
(great-circle) fallback.

STATUS: scaffold only. To be implemented during Task 4.

OPENROUTESERVICE_API_KEY is optional: without it, ground-transport distance
falls back to the great-circle distance between origin/destination.
"""
