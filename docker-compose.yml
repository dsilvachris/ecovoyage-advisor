version: "3.8"

services:
  ecovoyage-advisor:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
      - ADMIN_HTTP_USER=admin
      - ADMIN_HTTP_PASSWORD=changeme
    restart: unless-stopped