rest:
  # Enables the REST channel the frontend talks to at
  # /webhooks/rest/webhook — no extra config needed for local/dev.

# rasa.core.channels.socketio.SocketIOInput / other channels can be added
# here later if we decide the frontend needs push-style updates.
